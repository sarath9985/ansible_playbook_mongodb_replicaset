{% for host in groups['mongodb-slave'] %}
rs.add({
  host: "{{host}}",
  priority: {{hostvars[host]['priority']}},
  arbiterOnly: {{hostvars[host]['arbiterOnly']}}
})
{% endfor %}

printjson(rs.status())

